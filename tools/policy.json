{
  "describes": "The constants the workflow's prompts and tools read that belong to no one tool. Today there is one: the spelling of the tracker label that marks a product change's epic and every one of its tasks.",
  "whyThisFileExists": "A constant spelled in a register entry, six skills and an agent is eight copies, and the stale one is the one a reader finds. CLAUDE.md § Three kinds of file, and never a fourth puts every constant a tool or a prompt reads in a policy file under a key, with its reason beside it. `docs/decisions.md` § D-02 held the label's spelling only until this file existed; § D-03 moved it here.",
  "gatedBy": "npm run openspec:check (this file carries `specChangeLabel` and its `Means` sibling; every skill and agent that spells the label cites the key, and every one that cites the key spells its current value); npm run outcomes:record:selftest (an `intervention` block, where this file declares one, is held equal to the run-outcome schema's enum).",
  "whatItDoesNOTDo": "It does not hold the learning loop's constants, which are in tools/outcomes/policy.json, nor a rule about how a run proceeds, which lives in CLAUDE.md or in the skill that runs it. It declares no `intervention` block: the run-outcome schema's intervention enum is still the starter kit's starting vocabulary, and nothing but the schema reads it.",
  "provenance": "Written 2026-09-23 under D-03 (asdlc-openspec-4gp), which moved the label's spelling here out of D-02. Each later key carries its date and its evidence in its `Means` sibling.",

  "specChangeLabel": "spec-change",
  "specChangeLabelMeans": "The tracker label on a product change's epic and on every one of its tasks (docs/decisions.md § D-02, item 3). change-propose puts it on the epic and the children inherit it; the general sweeps of the queue exclude it (`bd ready --exclude-label` in the bead skill and the fan-out-work agent), and change-plan, change-build, change-verify and change-finalize find the epic by it. Changing it here strands every open change: relabel their epics and tasks in the same change, and respell the label in every skill and agent that cites this key, which `npm run openspec:check` names. Decided 2026-09-23 with D-02; moved here 2026-09-23 by D-03."
}
